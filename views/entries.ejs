<!DOCTYPE html>
<html lang="en">
    <head>
        <title><%= title %> </title>
        <link rel="stylesheet" href="/bootstrap.css" />
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />
        <script src="/bootstrap.js"></script>
    </head>
    <body>
        <div class="container">
            <%- include ('./partials/menu') %>
            <h1 class="text-center">Каталог битов</h1>
            <% entries.tracks.forEach((entry)=>{ %>
                <div class="card entry" style="width: 66rem;">
                    <%= entry.cover %>
                    <div class="card-body ">
                        <h5>Автор бита: <%= entry.author ? entry.author : 'Гость' %>
                            <% if (email === entry.author || role === 'admin') { %>
                            <a href="tracks/edit/<%= entry.id %>"><i class="fa-sharp fa-solid fa-pen" style="color: #000000;"></i></a>
                            <a href="tracks/delete/<%= entry.id %>"><i class="fa-solid fa-trash" style="color: #000000;"></i></a>
                            <% } %>
                        </h5>
                        <h5>Название бита: <%= entry.title %></h5>
                        <br>
                        <p class="card-text">
                            Теги (Жанр, BPM, тональность):
                        <%= entry.genre %>, <%= entry.bpm %> BPM, <%= entry.tone %>
                        <!-- Здесь контент будет -->
                        </p>
                        <br>
                    </div>
                    </br>
                </div>
                <br>
            <% }) %>
        </div>
    </body>
</html>
