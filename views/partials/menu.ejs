<nav class="navbar navbar-expand navbar-light bg-light justify-content-end">
  <div class="container container-fluid">
    <a
      class="navbar-brand bg-white p-1 rounded-circle overflow-hidden"
      href="/"
    >
      <img
        class=""
        src="../../public/img/logo.jpg"
        alt="Логотип"
        width="30"
        height="30"
      />
    </a>
    <% if (locals.user) { %>
    <div id="menu">
      <%# Меню пользователей, выполнивших вход%>
      <span class="name"><%= user.email %> </span>
      <% if (locals.role === 'admin') { %>
      <a class="badge rounded-pill bg-primary" href="/admin" aria-current="page"
        >Панель администратора<span class="visually-hidden">(current)</span></a
      >
      <% } else { %>
      <a href="/new">
        <button type="button" class="btn btn-light">Выложить бит</button>
      </a>
      <% } %>
      <a href="/logout">
        <button type="button" class="btn btn-light">Выйти</button>
      </a>
    </div>
    <% } else { %>
    <div class="" id="menu">
      <%# Меню анонимных пользователей %>
      <button
        type="button"
        class="btn btn-light"
        data-bs-toggle="modal"
        data-bs-target="#login"
      >
        Войти
      </button>
      <button
        type="button"
        class="btn btn-light"
        data-bs-toggle="modal"
        data-bs-target="#register"
      >
        Зарегистрироваться
      </button>
    </div>
    <% } %>
  </div>
</nav>
